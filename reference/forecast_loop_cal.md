# Internal forecast loop for calibration (without time series attributes)

Internal forecast loop for calibration (without time series attributes)

## Usage

``` r
forecast_loop_cal(object, xreg, h)
```
